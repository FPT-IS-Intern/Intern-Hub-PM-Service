spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/InternHubPM}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:123}
server:
  port: ${PORT:8080}
